import React from "react";
import Styled from "./styles.css";

interface Props {}

const SearchBar: React.FC<Props> = () => {
  return <Styled.SearchBarStyles></Styled.SearchBarStyles>;
};

export default SearchBar;
